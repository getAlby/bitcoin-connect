export type ComponentProps = {
  // FIXME: update these to use api methods
  // onConnect?(): void;
  // onConnecting?(): void;
  // onDisconnected?(): void;
  // onModalOpened?(): void;
  // onModalClosed?(): void;
};
